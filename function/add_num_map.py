## add some constant value to each member of the list.

values=[2,4,6,8]
#We convert the returned map object to
# a list data structure for printing purpose
addition=list(map(lambda x:x+10,values))
print(addition)